# NovaPredict Python pipeline — pinned for reproducible Workers Container builds.
# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
httpx==0.28.1
psycopg[binary]==3.2.9
python-dotenv==1.1.0
